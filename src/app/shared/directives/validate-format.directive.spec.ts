import { ValidateFormatDirective } from './validate-format.directive';

describe('ValidateFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateFormatDirective();
    expect(directive).toBeTruthy();
  });
});
